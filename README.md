# cr-analysis
 Silly game analysis using the Clash Royale API.

### Running locally
You'll need a developer account and API token from the [Clash Royale developer site](https://developer.clashroyale.com/#/getting-started).
Then, create `lib/auth.ts` exporting your token like so:
```ts
export const TOKEN = '...'
```
